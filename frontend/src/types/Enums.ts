export enum FormType {
  ADD,
  UPDATE,
}

export enum NotificationType {
  INFO,
  SUCCESS,
  ERROR,
}

export enum SuggesstionType {
  SONG,
  ARTIST,
}

export enum Sex {
  MALE = "MALE",
  FEMALE = "FEMALE",
  ORTHER = "ORTHER",
}
